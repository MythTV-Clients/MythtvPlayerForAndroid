#
# Build configuration for Circle CI
#
# See this thread for speeding up and caching directories: https://discuss.circleci.com/t/installing-android-build-tools-23-0-2/924
#

general:
  artifacts:
    - /home/ubuntu/MythtvPlayerForAndroid/app/build/outputs/apk/

machine:
  environment:
    ANDROID_HOME: /usr/local/android-sdk-linux
    ADB_INSTALL_TIMEOUT: 60
    GRADLE_OPTS: '-Dorg.gradle.jvmargs="-Xmx3072M -XX:+HeapDumpOnOutOfMemoryError"'
  java:
    version: openjdk8


#dependencies:
#  pre:
#    - touch app/google-services.json
#    - echo $GOOGLE_SERVICES_JSON > app/google-services.json
#    - echo y | android update sdk --no-ui --all --filter "tools,android-23,build-tools-24.0.0,platform-tools,extra-android-m2repository,extra-google-m2repository,extra-google-google_play_services,extra-android-support"
dependencies:
  pre:
    - touch app/google-services.json
    - echo $GOOGLE_SERVICES_JSON > app/google-services.json
    # Accept Android Developer License
    - mkdir -p "/usr/local/android-sdk-linux/licenses"
    - echo |set /p="8933bad161af4178b1185d1a37fbf41ea5269c55" > "/usr/local/android-sdk-linux/android-sdk-license"
    # Android SDK Platform 25
    - if [ ! -d "/usr/local/android-sdk-linux/platforms/android-25" ]; then echo y | android update sdk --no-ui --all --filter "android-25"; fi
    # Android SDK Build-tools, revision 25.0.1
    - if [ ! -d "/usr/local/android-sdk-linux/build-tools/25.0.1" ]; then echo y | android update sdk --no-ui --all --filter "build-tools-25.0.1"; fi
    # Android Support Repository, revision 35 / Local Maven repository for Support Libraries
    - if [ ! -d "/usr/local/android-sdk-linux/extras/android/m2repository/com/android/support/design/25.0.1" ]; then echo y | android update sdk --no-ui --all --filter "extra-android-m2repository,extra-google-m2repository,extra-google-google_play_services,extra-android-support"; fi

  cache_directories:
    - /usr/local/android-sdk-linux/platforms/android-25
    - /usr/local/android-sdk-linux/build-tools/25.0.1
    - /usr/local/android-sdk-linux/extras/android/m2repository

test:
#  pre:
#    - android list targets
#    - echo no | android create avd -n custom-android22-googleapis -t "android-22" --abi google_apis/armeabi-v7a
#    - echo 'vm.heapSize=512' >> ~/.android/avd/custom-android22-googleapis.ini
#    - echo 'hw.ramSize=2048' >> ~/.android/avd/custom-android22-googleapis.ini
#    - echo 'hw.gpu.enabled=yes' >> ~/.android/avd/custom-android22-googleapis.ini
#    - cat ~/.android/avd/custom-android22-googleapis.ini
#    - emulator -avd custom-android22-googleapis -no-audio -no-window :
#        background: true
#        parallel: true
#    - circle-android wait-for-boot
#    - sleep 30
#    - adb shell input keyevent 82
#    - adb shell svc power stayon true
#    - adb shell settings put global window_animation_scale 0
#    - adb shell settings put global transition_animation_scale 0
#    - adb shell settings put global animator_duration_scale 0
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/build/test-results/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;

  override:
    - git checkout app/google-services.json
    - ./gradlew testDevDebugUnitTest -PdisablePreDex --console=plain --info  # connectedDevDebugAndroidTest

  post:
    - cp -r app/build/outputs $CIRCLE_ARTIFACTS
#    - cp -r app/build/outputs/androidTest-results/ $CIRCLE_TEST_REPORTS
    - cp -r app/build/test-results/devDebug/ $CIRCLE_TEST_REPORTS
